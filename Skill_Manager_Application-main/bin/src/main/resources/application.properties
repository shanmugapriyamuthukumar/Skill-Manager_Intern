spring.application.name=SkillManagementApplication


# --- MySQL ---
# Make sure the DB name is IN the URL
spring.datasource.url=jdbc:mysql://localhost:3306/skill_mgmt?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=root@39

# Remove this line entirely (or keep only one simple statement if you must)
# spring.datasource.hikari.connectionInitSql=SET NAMES utf8mb4; USE skill_mgmt;

# If you still want it, keep it as ONE statement only (optional):
# spring.datasource.hikari.connectionInitSql=SET NAMES utf8mb4

# --- JPA/Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# Optional (avoids deprecation warnings and is explicit)
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# --- JWT ---
app.jwt.secret=wgmAW8tMBu3Zo4e6qMcCGZDYRi/JpGECkyeN+YB2tc8=
app.jwt.expiration-ms=86400000
app.jwt.clock-skew-seconds=60

# --- Admin bootstrap ---
app.admin.name=System Admin
app.admin.email=admin@sms.local
app.admin.password=Admin@123

# --- CORS (Angular dev) ---
app.cors.allowed-origins=http://localhost:4200

server.port = 9092